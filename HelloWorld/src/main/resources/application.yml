student:
  name: hello
  age: 22
  course: english,mathematics,chinese

spring:
  datasource:
    continue-on-error: false
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/demo?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
    schema: classpath:database/create.sql
    data: classpath:database/import.sql
    initialization-mode: never
  jpa:
    show-sql: true

