server:
  port: 8080

spring:
  application:
    name: service-client

eureka:
  client:
    serviceUrl:
      defaultZone: http://admin:admin@localhost:8761/eureka/

feign:
  hystrix:
    enabled: true